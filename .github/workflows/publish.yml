name: Publish

on:
  push:
    branches: [main]
    paths:
      - "packages/*/package.json"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Build & Publish
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: https://registry.npmjs.org
          scope: "@better-s3"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm turbo run build --filter="./packages/*"

      - name: Type check packages
        run: pnpm turbo run check-types --filter="./packages/*"

      - name: Configure npm authentication
        run: |
          echo "//registry.npmjs.org/:_authToken=$NODE_AUTH_TOKEN" > ~/.npmrc
          echo "@better-s3:registry=https://registry.npmjs.org" >> ~/.npmrc

      - name: Validate npm token
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is not set"
            exit 1
          fi
          npm whoami

      - name: Publish @better-s3/server
        run: |
          VERSION=$(node -p "require('./packages/better-s3-server/package.json').version")
          if npm view @better-s3/server@$VERSION version > /dev/null 2>&1; then
            echo "server@$VERSION already published, skipping."
          else
            pnpm --filter @better-s3/server publish --access public --no-git-checks
          fi

      - name: Publish @better-s3/react
        run: |
          VERSION=$(node -p "require('./packages/better-s3-react/package.json').version")
          if npm view @better-s3/react@$VERSION version > /dev/null 2>&1; then
            echo "react@$VERSION already published, skipping."
          else
            pnpm --filter @better-s3/react publish --access public --no-git-checks
          fi

      - name: Publish @better-s3/ui
        run: |
          VERSION=$(node -p "require('./packages/better-s3-ui/package.json').version")
          if npm view @better-s3/ui@$VERSION version > /dev/null 2>&1; then
            echo "ui@$VERSION already published, skipping."
          else
            pnpm --filter @better-s3/ui publish --access public --no-git-checks
          fi
